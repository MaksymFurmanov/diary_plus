const StockTable = ({type, items}) => {
    const itemsList = items.map((item, index) => {
        if(type === "entry"){

        } else if(type === "output") {

        }
    })

    return <ul>

    </ul>
}

export default StockTable